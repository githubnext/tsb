# Skill: lint-policy-review

Use when lint output looks like a repo policy or configuration question rather
than a local mechanical mistake.

Separate auto-fixable style failures from rule conflicts, generated-file
exceptions, and policy decisions. Recommend a code fix, config change, or human
decision with evidence.
