# Skill: api-contract-gate-repair

Use when API, schema, protocol, compatibility, or public contract checks block
mergeability.

Identify the changed contract surface, the failing compatibility evidence, and
whether the fix belongs in implementation, tests, generated artifacts, docs, or
a human-owned policy decision.
