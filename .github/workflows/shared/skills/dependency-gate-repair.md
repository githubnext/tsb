# Skill: dependency-gate-repair

Use when package manager, lockfile, dependency update, license, or supply-chain
gates block mergeability.

Inspect package-manager evidence and lockfile state. Prefer repo-native install
or lockfile update commands and avoid broad dependency upgrades unless the gate
requires them.
